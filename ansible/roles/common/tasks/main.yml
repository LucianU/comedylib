---
- name: create /var/www
  file: path=/var/www
        state=directory
  sudo: yes

- name: create dirs
  file: path={{ item }}
        owner={{ app_user }}
        group={{ app_group }}
        state=directory
  with_items:
    - "{{ project_log_path }}"
    - "{{ project_run_path }}"
  sudo: yes

- include: user.yml
- include: ssh_keys.yml
- include: packages.yml
